FROM amazoncorretto:17
ARG JAR_FILE=backend/build/libs/*.jar

ENV SPRING_PROFILES_ACTIVE=prod

# 호스트 머신의 빌드된 JAR 파일을 컨테이너로 복사
COPY ${JAR_FILE} app.jar

ENV PROFILE=${PROFILE}

ARG DB_HOST
ARG DB_USERNAME
ARG DB_PASSWORD

ARG SLACK_SERVER_ERROR_URL
ARG SLACK_RSS_READER_URL

ENV DB_HOST=$DB_HOST
ENV DB_USERNAME=$DB_USERNAME
ENV DB_PASSWORD=$DB_PASSWORD

ENV SLACK_SERVER_ERROR_URL=$SLACK_SERVER_ERROR_URL
ENV SLACK_RSS_READER_URL=$SLACK_RSS_READER_URL

# 포트 설정 (Spring Boot 애플리케이션이 사용하는 포트에 맞게 변경)
EXPOSE 8080

# 애플리케이션 실행
CMD ["java", "-jar", "app.jar"]